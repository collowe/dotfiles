;;; package --- Summary
;;; Commentary:
; early initialisation configuration

;;; Code:
; disable use of package.el
(setq package-enable-at-startup nil)
; report loading and configuration details

(provide 'early-init)
;;; early-init.el ends here
